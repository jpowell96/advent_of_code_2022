// Question to answer: In how many assignment pairs does one range fully contain the other?

export function countOverlappingPairs(assignments: number[][]) : number {
    return -1;
}

// Returns true if a overlaps b, or if b overlaps a
export function overlaps(assignmentA: number[], assignmentB: number[]) : boolean {
    return false;
}